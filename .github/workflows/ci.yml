name: build

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: g++ -std=c++17 main.cpp

      - run: printf "music\nnothing\n" | ./a.out

