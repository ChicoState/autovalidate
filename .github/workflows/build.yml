name: compile

on:
    push:
        branches: 'devops'
    pull_request:
        branches: [main]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: compile
              run: g++ -std=c++17 main.cpp
