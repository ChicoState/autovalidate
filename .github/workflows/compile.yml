name: compile

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "devops" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Compile
        run: g++ -std=c++17 main.cpp
