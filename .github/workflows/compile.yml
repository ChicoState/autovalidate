name: compile

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ devops ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      
      - name: Build (C++17)
        uses: actions/checkout@v4
        run: g++ -std=c++17 main.cpp -o app